import React from "react";

const form = () => {
  return (
    <div>
      <form>
        <input type="text" />
        <input type="email" />
        <textarea></textarea>
        <button>Order</button>
      </form>
    </div>
  );
};

export default form;
